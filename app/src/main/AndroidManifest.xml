<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projekt">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Projekt">
        <activity android:name=".KosztorysActivity"></activity>
        <activity android:name=".LevelActivity" />
        <activity android:name=".FlashlightActivity" />
        <activity android:name=".RobociznaActivity" />
        <activity android:name=".PricePaintingActivity" />
        <activity android:name=".DataActivity" />
        <activity android:name=".CalendarActivity" />
        <activity android:name=".CalculatorActivity" />
        <activity android:name=".NoteEditorActivity" />
        <activity android:name=".NotebookActivity" />
        <activity
            android:name=".SplashScreen"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
    </application>

</manifest>